# React Redux Example Build

Install Pacakges:
`npm install` or `yarn`

Run Locally:
`npm start` or `yarn start`

Production Build:
`npm run build` or `yarn build`

Run Test:
`npm run test` or `yarn test`

